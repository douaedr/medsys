package com.hospital.appointment.entity;

/**
 * Statut d'une notification email.
 * Migré depuis Models/Notification.cs (.NET).
 */
public enum NotificationStatus {
    Pending,
    Sent,
    Failed
}
